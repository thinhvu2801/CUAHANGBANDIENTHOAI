@model IEnumerable<CHBHDT63131330.Models.PhanQuyen>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Phân quyền</h2>

<p>
    @Html.ActionLink("Tạo phân quyền", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenQuyen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenQuyen)
        </td>
        <td>
            <button class="btn btn-success">
                @Html.ActionLink("Sửa", "Edit", new { id = item.IDQuyen }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-primary">
                @Html.ActionLink("Xem", "Details", new { id = item.IDQuyen }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-danger">
                @Html.ActionLink("Xóa", "Delete", new { id = item.IDQuyen }, new { @style = "color:white;" })
            </button>
        </td>
    </tr>
}

</table>
