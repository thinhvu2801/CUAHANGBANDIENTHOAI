@model IEnumerable<CHBHDT63131330.Models.NhaCungCap>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Nhà cung cấp</h2>

<p>
    @Html.ActionLink("Tạo nhà cung cấp", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNCC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNCC)
        </td>
        <td>
            <button class="btn btn-success">
                @Html.ActionLink("Sửa", "Edit", new { id = item.MaNCC }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-primary">
                @Html.ActionLink("Xem", "Details", new { id = item.MaNCC }, new { @style = "color:white;" })
            </button>
            <button class="btn btn-danger">
                @Html.ActionLink("Xóa", "Delete", new { id = item.MaNCC }, new { @style = "color:white;" })
            </button>
        </td>
    </tr>
}

</table>
